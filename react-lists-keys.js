/*
📘 Rendering Lists & Keys:
We can use map() to render a list of elements.
Each child in a list should have a unique "key" prop.

Example:
const items = ['a', 'b'];
{items.map((item, i) => <li key={i}>{item}</li>)}
*/
